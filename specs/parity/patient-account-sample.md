# Patient Account Parity — Single Sample

**Procedure:** `accounting.patientLasikSummary` (`getPatientLasikSummary`).

**Legacy OP export:** Not available in-repo for this sample. **Legacy-equivalent source:**
aggregated `SELECT` over `PAJRNRCVH` INNER JOIN `PAPAT_SRV` with the same predicates as
`buildPatientLasikSummarySql` (Lasik section, non-cancelled header/lines, one `PAT_CD`).

## Parameters

| Field | Value |
|-------|-------|
| patientCode | `1354` |
| sectionCode | `15` |
| fromDate / toDate | *(none — full Lasik ledger for patient)* |

## Legacy-equivalent totals query (MSSQL)

```sql
SELECT
  COUNT(DISTINCT CONCAT(':', CONVERT(varchar(12), h.TR_TY), ':', LTRIM(RTRIM(CONVERT(varchar(40), h.TR_NO))))) AS receiptCount,
  COUNT_BIG(*) AS serviceCount,
  SUM(ISNULL(s.QTY, 0) * ISNULL(s.PRC, 0)) AS totalGross,
  SUM(ISNULL(s.DISC_VL, 0)) AS totalDiscount,
  SUM(ISNULL(s.PA_VL, 0)) AS totalPaid,
  SUM(ISNULL(s.CA_VL, 0)) AS totalCompany,
  MAX(h.TR_DT) AS lastTransactionDate
FROM PAJRNRCVH h
JOIN PAPAT_SRV s
  ON h.SEC_CD = s.SEC_CD
 AND h.TR_TY = s.TR_TY
 AND h.TR_NO = s.TR_NO
WHERE h.SEC_CD = @secCd
  AND h.PAT_CD = @patientCode
  AND h.CNCL IS NULL
  AND s.CNCL IS NULL
```

Bindings: `@secCd` = section code (15), `@patientCode` = sample patient.

## Comparison

| Metric | API | Legacy SQL | Delta | Status |
|--------|-----|------------|-------|--------|
| patientCode | 1354 | 1354 | — | PASS |
| receipt count | 1 | 1 | — | PASS |
| service count | 1 | 1 | — | PASS |
| totalGross | 360.00 | 360.00 | 0.0000 | PASS |
| totalDiscount | 0.00 | 0.00 | 0.0000 | PASS |
| totalPaid / patient amount | 360.00 | 360.00 | 0.0000 | PASS |
| totalPatientAmount (API mirrors totalPaid) | 360.00 | 360.00 | 0.0000 | PASS |
| totalCompanyAmount | 0.00 | 0.00 | 0.0000 | PASS |
| lastTransactionDate (date) | 2026-04-30 | 2026-04-30 | — | PASS |

**Verdict: PASS**

---

*Generated by `scripts/accounting/parity-check.ts`.*