import * as React from 'react';

/**
 * MenubarSub — from selrs-ui@0.0.1.
 */
export interface MenubarSubProps {
  children?: React.ReactNode;
  open?: boolean;
  defaultOpen?: boolean;
}

export declare const MenubarSub: React.ComponentType<MenubarSubProps>;
