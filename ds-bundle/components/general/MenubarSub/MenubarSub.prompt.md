MenubarSub from selrs-ui. Use via `window.SELRSUI.MenubarSub` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface MenubarSubProps {
  children?: React.ReactNode;
  open?: boolean;
  defaultOpen?: boolean;
}
```

## Related

`MenubarSubContent`, `MenubarSubTrigger`
