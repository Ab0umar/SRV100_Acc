FormField from selrs-ui. Use via `window.SELRSUI.FormField` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface FormFieldProps {
  label: string;
  error?: string;
  required?: boolean;
  children: React.ReactNode;
  helperText?: string;
}
```
