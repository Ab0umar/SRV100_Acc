DropdownMenuSub from selrs-ui. Use via `window.SELRSUI.DropdownMenuSub` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface DropdownMenuSubProps {
  children?: React.ReactNode;
  open?: boolean;
  defaultOpen?: boolean;
}
```

## Related

`DropdownMenuSubContent`, `DropdownMenuSubTrigger`
