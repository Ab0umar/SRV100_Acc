Popover from selrs-ui. Use via `window.SELRSUI.Popover` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface PopoverProps {
  children?: React.ReactNode;
  open?: boolean;
  defaultOpen?: boolean;
  modal?: boolean;
}
```

## Related

`PopoverAnchor`, `PopoverContent`, `PopoverTrigger`
