HoverCard from selrs-ui. Use via `window.SELRSUI.HoverCard` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface HoverCardProps {
  children?: React.ReactNode;
  open?: boolean;
  defaultOpen?: boolean;
  openDelay?: number;
  closeDelay?: number;
}
```

## Related

`HoverCardContent`, `HoverCardTrigger`
