MenubarMenu from selrs-ui. Use via `window.SELRSUI.MenubarMenu` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface MenubarMenuProps {
  value?: string;
  children?: React.ReactNode;
  __scopeMenubar?: Scope;
}
```
