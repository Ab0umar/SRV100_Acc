ContextMenuSub from selrs-ui. Use via `window.SELRSUI.ContextMenuSub` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface ContextMenuSubProps {
  children?: React.ReactNode;
  open?: boolean;
  defaultOpen?: boolean;
}
```

## Related

`ContextMenuSubContent`, `ContextMenuSubTrigger`
