CalendarDayButton from selrs-ui. Use via `window.SELRSUI.CalendarDayButton` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface CalendarDayButtonProps {
  /** The day to render. */
  day: CalendarDay;
  /** The modifiers to apply to the day. */
  modifiers: Modifiers;
  className?: string;
  id?: string;
  style?: React$1.CSSProperties;
  children?: React.ReactNode;
}
```
