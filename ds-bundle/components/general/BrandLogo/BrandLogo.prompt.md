BrandLogo from selrs-ui. Use via `window.SELRSUI.BrandLogo` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface BrandLogoProps {
  className?: string;
  imgClassName?: string;
  fallbackClassName?: string;
}
```
