AppFormField from selrs-ui. Use via `window.SELRSUI.AppFormField` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface AppFormFieldProps {
  label: string;
  error?: string;
  required?: boolean;
  children: React.ReactNode;
  helperText?: string;
}
```
