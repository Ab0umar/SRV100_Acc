Sheet from selrs-ui. Use via `window.SELRSUI.Sheet` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface SheetProps {
  children?: React.ReactNode;
  open?: boolean;
  defaultOpen?: boolean;
  modal?: boolean;
}
```

## Related

`SheetClose`, `SheetContent`, `SheetDescription`, `SheetFooter`, `SheetHeader`, `SheetTitle`, `SheetTrigger`
