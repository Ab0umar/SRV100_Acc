OfflinePageState from selrs-ui. Use via `window.SELRSUI.OfflinePageState` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface OfflinePageStateProps {
  title: string;
  body: string;
  retryLabel?: string;
  onRetry?: () => void;
}
```
