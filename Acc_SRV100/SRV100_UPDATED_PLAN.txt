SRV100 UPDATED PLAN

Goal:
Replace OP.exe with a unified web app using same SQL Server database.

Core Concept:
- Use existing SQL tables (no schema change)
- Build web UI only
- Queries live in backend services

Minimal Tables:
PAPATMF, MDTEAM, SRVCMF, PAJRNRCVH, PAPAT_SRV

Modules:
1. Patients
2. Doctors
3. Services
4. Receipts
5. Revenue Reports

Rules:
- Do NOT use unused tables
- Do NOT rebuild full OP
- Only implement minimal needed features
- Use same joins and logic as OP

Architecture:
Frontend → API → SQL Server

Year Handling:
Login → select year → connect to op{year}
