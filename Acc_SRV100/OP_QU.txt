A. Minimal Tables Needed

- PAPATMF
  - patient master
- MDTEAM
  - doctor master
- SRVCMF
  - service master
- PAJRNRCVH
  - receipt header
- PAPAT_SRV
  - receipt/service details


B. Real SQL Queries

1. Patient query

Source:
- D:\OP\طباعة الأدلة\PAPATMF.rtm

Real report SELECT from OP:
```sql
SELECT PAPATMF.PAT_CD, PAPATMF.PAT_NM_AR,
       PAPATMF.DT, PAPATMF.BDT, PAPATMF.AGE,
       PAPATMF.SUM_CD, PAPATMF.GNDR, PAPATMF.NAT,
       PAPATMF.IDNO, PAPATMF.IDTY, PAPATMF.IDSRC,
       PAPATMF.IDISUDT, PAPATMF.RLTN,
       PAPATMF.POBOX, PAPATMF.ZIP, PAPATMF.ADDRS,
       PAPATMF.OCUPTN, PAPATMF.WRKWHR,
       PAPATMF.TEL1, PAPATMF.TEL2, PAPATMF.TEL3,
       PAPATMF.DR_CD, PAPATMF.RMRK1,
       PAPATMF.DRS_CD, PAPATMF.PATST,
       PAPATMF.BLDCLSS, PAPATMF.CHLD_NO,
       PAPATMF.GOVRN, PAPATMF.TOWN, PAPATMF.RLGN,
       PAPATMF.FILE_PLC, PAPATMF.PRC_DGR,
       PAPATMF.SMOK_STAT, PAPATMF.FILE_PLC_DT
FROM PAPATMF PAPATMF
```

Minimal inquiry version using the same table/fields:
```sql
SELECT PAT_CD, PAT_NM_AR, DT, BDT, AGE, ADDRS, TEL1, TEL2, TEL3, DRS_CD
FROM PAPATMF
WHERE PAT_CD = @PAT_CD OR PAT_NM_AR LIKE N'%' + @NAME + N'%'
ORDER BY PAT_CD
```

Patient link to services and receipts:
```sql
SELECT h.SEC_CD, h.TR_TY, h.TR_NO, h.TR_DT, h.TOTL, h.DISC, h.PA_VL,
       d.SRV_CD, d.QTY, d.PRC, d.DISC_VL, d.PA_VL AS LINE_PA_VL
FROM PAJRNRCVH h
JOIN PAPAT_SRV d
  ON h.SEC_CD = d.SEC_CD
 AND h.TR_TY  = d.TR_TY
 AND h.TR_NO  = d.TR_NO
WHERE h.PAT_CD = @PAT_CD
ORDER BY h.TR_DT DESC, h.TR_NO DESC
```

2. Doctor query

Source:
- D:\OP\طباعة الأدلة\MDTEAM.rtm

Real report SELECT from OP:
```sql
SELECT MDTEAM.CODE, MDTEAM.PHNM_AR,
       RSRV_WRKTIMS.FRMDT, RSRV_WRKTIMS.TODT,
       RSRV_WRKSCM.TIN1, RSRV_WRKSCM.TOUT1,
       dbo.GetDPt_NM(dpt_no) AS dbo_GetDPt_NM_dpt_no
FROM ((MDTEAM MDTEAM
      INNER JOIN RSRV_WRKTIMS RSRV_WRKTIMS
        ON RSRV_WRKTIMS.CODE = MDTEAM.CODE)
      INNER JOIN RSRV_WRKSCM RSRV_WRKSCM
        ON RSRV_WRKSCM.SCM_ID = RSRV_WRKTIMS.SCM)
```

Minimal inquiry version using the doctor master only:
```sql
SELECT CODE, PHNM_AR, DPT_NO, TEL1, TEL2, TEL3, ADDRS, STAT
FROM MDTEAM
WHERE CODE = @CODE OR PHNM_AR LIKE N'%' + @NAME + N'%'
ORDER BY CODE
```

Doctor link to services:
```sql
SELECT s.SRV_BY1 AS doctor_code, m.PHNM_AR AS doctor_name,
       s.SRV_CD, s.QTY, s.PRC, (ISNULL(s.QTY,0) * ISNULL(s.PRC,0)) AS gross_value
FROM PAPAT_SRV s
LEFT JOIN MDTEAM m
  ON m.CODE = s.SRV_BY1
WHERE s.SRV_BY1 = @DOCTOR_CODE
```

3. Service query

Source:
- D:\OP\طباعة الأدلة\SRV.rtm

Real report SELECT from OP:
```sql
SELECT SRVCMF.SRV_CD, SRVCMF.SRV_NM_AR,
       dbo.getapp_nm(prcgrp_id,1055) AS dbo_getapp_nm_prcgrp_id,
       dbo.getapp_nm(prcgrp_id,1056) AS dbo_getapp_nm_prcgrp_id_2,
       dbo.getdpt_nm(dpt_no) AS dbo_getdpt_nm_dpt_no
FROM SRVCMF SRVCMF
```

Minimal inquiry version using actual service fields:
```sql
SELECT SRV_CD, SRV_NM_AR, DPT_NO, PRC1, PRC2, PRC3, DISC, DISC_VL
FROM SRVCMF
WHERE SRV_CD = @SRV_CD OR SRV_NM_AR LIKE N'%' + @NAME + N'%'
ORDER BY SRV_CD
```

4. Receipt query

Source:
- D:\OP\تقرير الرمد.rtm

Real report SELECT from OP:
```sql
SELECT 1 grp,
       pajrnrcvh.sec_cd,
       pajrnrcvh.pat_cd,
       pajrnrcvh.nam,
       pajrnrcvh.tr_no,
       pajrnrcvh.tr_dt,
       pajrnrcvh.tr_ty,
       pajrnrcvh.shft,
       papat_srv.ca_cd,
       papat_srv.disc_vl,
       papat_srv.srv_cd,
       papat_srv.srv_by1,
       papat_srv.qty,
       papat_srv.prc,
       pajrnrcvh.totl,
       (papat_srv.qty * papat_srv.prc) AS tot,
       (papat_srv.pa_vl - papat_srv.disc_vl) AS pa_vl,
       (papat_srv.ca_vl - disc_ca) AS ca_vl,
       pajrnrcvh.enteredby,
       (SELECT dpt_nm_ar FROM dept WHERE dpt_no = papat_srv.sec_cd) AS dept_nm,
       (SELECT srv_nm_ar FROM srvcmf WHERE srv_cd = papat_srv.srv_cd) AS srv_nm,
       (SELECT ca_nm_ar FROM cmpmf WHERE ca_cd = papat_srv.ca_cd) AS ca_nm,
       (SELECT dscr_ar FROM appcodes WHERE id = 1015 AND seq = pajrnrcvh.shft) AS shft_1,
       CASE pajrnrcvh.TR_TY
         WHEN 1 THEN N'نقدى'
         WHEN 5 THEN N'آجل'
         WHEN 6 THEN N'نزلاء'
         WHEN 8 THEN N'مسترد'
       END AS TRTYN
FROM papat_srv, pajrnrcvh
WHERE pajrnrcvh.sec_cd = papat_srv.sec_cd
  AND pajrnrcvh.tr_no  = papat_srv.tr_no
  AND pajrnrcvh.tr_ty  = papat_srv.tr_ty
```

Minimal receipt inquiry version:
```sql
SELECT h.SEC_CD, h.TR_TY, h.TR_NO, h.TR_DT, h.PAT_CD, h.NAM,
       h.TOTL, h.DISC, h.PA_VL,
       d.SRV_CD, d.SRV_BY1, d.QTY, d.PRC, d.DISC_VL, d.PA_VL AS LINE_PA_VL
FROM PAJRNRCVH h
JOIN PAPAT_SRV d
  ON h.SEC_CD = d.SEC_CD
 AND h.TR_TY  = d.TR_TY
 AND h.TR_NO  = d.TR_NO
WHERE h.SEC_CD = @SEC_CD
  AND h.TR_NO  = @TR_NO
  AND h.TR_TY  = @TR_TY
ORDER BY d.SRV_CD
```

5. Revenue query

Source:
- D:\OP\اطباء.rtm

Real report SELECT from OP:
```sql
SELECT srv_by1, srv_cd, qty, prc,
       (qty * prc) AS tot,
       (SELECT PHNM_AR FROM mdteam WHERE code = papat_srv.srv_by1) AS DOCNM,
       (SELECT ca_nm_AR FROM cmpmf WHERE ca_cd = papat_srv.ca_cd) AS CANM,
       (SELECT srv_nm_AR FROM srvcmf WHERE srv_cd = papat_srv.srv_cd) AS srvnm
FROM papat_srv, PAJRNRCVH
WHERE srv_by1 <> ' '
  AND (1 <> 1)
  AND papat_srv.cncl IS NULL
  AND pajrnrcvh.tr_no  = papat_srv.tr_no
  AND pajrnrcvh.sec_cd = papat_srv.sec_cd
  AND pajrnrcvh.tr_ty  = papat_srv.tr_ty
ORDER BY srv_by1, srvnm
```

Minimal revenue summary version from the same logic:
```sql
SELECT s.SRV_BY1 AS doctor_code,
       m.PHNM_AR AS doctor_name,
       s.SRV_CD,
       c.SRV_NM_AR AS service_name,
       CAST(h.TR_DT AS date) AS tr_date,
       SUM(ISNULL(s.QTY,0)) AS total_qty,
       SUM(ISNULL(s.QTY,0) * ISNULL(s.PRC,0)) AS gross_value,
       SUM(ISNULL(s.PA_VL,0)) AS paid_value
FROM PAPAT_SRV s
JOIN PAJRNRCVH h
  ON h.SEC_CD = s.SEC_CD
 AND h.TR_TY  = s.TR_TY
 AND h.TR_NO  = s.TR_NO
LEFT JOIN MDTEAM m
  ON m.CODE = s.SRV_BY1
LEFT JOIN SRVCMF c
  ON c.SRV_CD = s.SRV_CD
WHERE ISNULL(s.SRV_BY1,'') <> ''
  AND s.CNCL IS NULL
GROUP BY s.SRV_BY1, m.PHNM_AR, s.SRV_CD, c.SRV_NM_AR, CAST(h.TR_DT AS date)
ORDER BY s.SRV_BY1, s.SRV_CD, tr_date
```


C. Required Fields Per Screen

1. Patients

- From PAPATMF:
  - PAT_CD
  - PAT_NM_AR
  - DT
  - BDT
  - AGE
  - ADDRS
  - TEL1
  - TEL2
  - TEL3
  - DRS_CD

2. Doctors

- From MDTEAM:
  - CODE
  - PHNM_AR
  - DPT_NO
  - TEL1
  - TEL2
  - TEL3
  - ADDRS
  - STAT

3. Services

- From SRVCMF:
  - SRV_CD
  - SRV_NM_AR
  - DPT_NO
  - PRC1
  - PRC2
  - PRC3
  - DISC
  - DISC_VL

4. Receipts

- Header from PAJRNRCVH:
  - SEC_CD
  - TR_TY
  - TR_NO
  - TR_DT
  - PAT_CD
  - NAM
  - TOTL
  - DISC
  - PA_VL
  - ENTEREDBY

- Details from PAPAT_SRV:
  - SRV_CD
  - SRV_BY1
  - QTY
  - PRC
  - DISC_VL
  - PA_VL
  - CA_VL

5. Revenue

- From PAPAT_SRV:
  - SRV_BY1
  - SRV_CD
  - QTY
  - PRC
  - DISC_VL
  - PA_VL

- Lookup fields:
  - MDTEAM.PHNM_AR
  - SRVCMF.SRV_NM_AR
  - PAJRNRCVH.TR_DT


D. UI Structure

1. Patient screen

Source:
- D:\OP\طباعة الأدلة\PAPATMF.rtm

Visible column order in report layout:
- PAT_CD
- PAT_NM_AR
- DT
- BDT
- AGE
- PATST display name
- CHLD_NO
- BLDCLSS display name
- GOVRN display name
- TOWN display name

Grouping:
- None

Totals:
- None

2. Doctor screen

Source:
- D:\OP\طباعة الأدلة\MDTEAM.rtm

Visible column order:
- CODE
- PHNM_AR
- department name from `dbo.GetDPt_NM(dpt_no)`
- FRMDT
- TODT
- TIN1
- TOUT1

Grouping:
- None

Totals:
- None

Important:
- OP doctor print is mixed with scheduling tables `RSRV_WRKTIMS` and `RSRV_WRKSCM`
- For the minimal accounting web app, only `CODE`, `PHNM_AR`, and `DPT_NO` are core

3. Receipt screen

Source:
- D:\OP\تقرير الرمد.rtm

Header fields shown:
- department name
- TR_NO
- TRTYN
- TR_DT
- NAM
- SRV_BY1
- SHFT_1
- CA_NM
- TOTL
- ENTEREDBY

Body column order:
- SRV_NM
- QTY
- PRC
- TOT = QTY * PRC
- DISC_VL
- PA_VL
- CA_VL

Grouping:
- No group bands in the visible print structure
- One receipt header, then detail lines

Totals:
- The header shows the receipt total `TOTL`
- Line values show service totals and paid/discount splits

4. Revenue screen

Sources:
- D:\OP\اطباء.rtm
- D:\OP\REPORTS\TRF_DRSRV1.RTM

From `اطباء.rtm` visible logic:
- grouped by doctor `docnm`
- secondary grouping by service `srvnm`

Visible logical columns:
- doctor code `srv_by1`
- doctor name `docnm`
- service code `srv_cd`
- service name `srvnm`
- quantity `qty`
- price `prc`
- total `tot`

Grouping:
- Group 1 by doctor
- Group 2 by service

Totals:
- Per service:
  - count of rows
  - sum of `tot`
- Per doctor:
  - count of rows
  - sum of `tot`

From `TRF_DRSRV1.RTM` visible detail order:
- TR_NO
- PAT_CD
- PAT_NM
- DT
- QTY
- PRC
- SRV_CD
- SRV_NM

From `TRF_DRSRV1.RTM` grouping:
- Group 1 by `SRV_BY`
- Group 4 by `SRV_CD`

From `TRF_DRSRV1.RTM` totals:
- Per doctor:
  - sum PRC
  - sum QTY
  - sum NET_VAL
  - sum DISC_VL
  - sum PA_VL
- Per service:
  - sum PRC
  - sum QTY


E. Print Layout

1. Patient print layout

Header:
- report title `بيانات المرضى`

Body:
- one grid row per patient
- columns in this visual order:
  - file number / PAT_CD
  - patient name
  - file open date
  - birth date
  - age
  - marital status display
  - children count
  - blood class display
  - governorate display
  - town display

Grouping:
- none

Totals:
- none

Footer:
- empty

2. Doctor print layout

Header:
- report title `دليل فريق العمل`

Body:
- one row per doctor schedule record
- columns:
  - doctor code
  - doctor name
  - department
  - from date
  - to date
  - check-in time
  - check-out time

Grouping:
- none

Totals:
- none

Footer:
- empty

3. Receipt print layout

Source:
- D:\OP\تقرير الرمد.rtm

Header:
- clinic/hospital title
- department name
- receipt number
- payment type
- receipt date
- patient name
- doctor code
- shift
- payer/company name
- total amount
- entered by

Body:
- service table with columns:
  - service name
  - quantity
  - price
  - gross line total
  - discount
  - patient share
  - company share

Grouping:
- not grouped by doctor/service/date
- this is a single receipt page with its detail rows

Totals:
- overall receipt total is shown in the header as `TOTL`
- line splits are shown per row

Footer:
- no strong signature/footer structure found in the inspected report

4. Revenue print layout

Source:
- D:\OP\اطباء.rtm

Header:
- hospital/clinic title
- report title `مستحقات الأطباء`
- date range fields

Body:
- grouped listing by doctor, then by service
- visible detail/service subtotal columns:
  - service code
  - service name
  - quantity
  - price/value
  - total

Grouping:
- outer group: doctor
- inner group: service

Totals:
- service subtotal:
  - count of rows
  - sum of total
- doctor subtotal:
  - count of rows
  - sum of total

Footer:
- no signatures found

Source:
- D:\OP\REPORTS\TRF_DRSRV1.RTM

Header:
- hospital name
- address lines
- report title `تقرير مستحقات الأطباء`
- date range
- page number / print date

Body:
- columns:
  - receipt number
  - patient code
  - patient name
  - date
  - quantity
  - price
  - service code
  - service name

Grouping:
- by doctor
- then by service

Totals:
- doctor subtotal:
  - PRC
  - QTY
  - NET_VAL
  - DISC_VL
  - PA_VL
- service subtotal:
  - PRC
  - QTY

Footer:
- no signature fields found


F. What to IGNORE

- RSRV_WRKTIMS
- RSRV_WRKSCM
- PAPAT_PAY
- CMPMF
- APPCODES
- DEPT
- any scheduling logic
- any insurance/claim/payment-only receipt templates
- any stock/pharmacy/lab/inventory tables
- any year switching logic
- any triggers, posting, GL, or cancellation procedures

Reason to ignore:
- they are not part of the minimal working inquiry + receipt + revenue accounting system built only on:
  - PAPATMF
  - MDTEAM
  - SRVCMF
  - PAJRNRCVH
  - PAPAT_SRV
