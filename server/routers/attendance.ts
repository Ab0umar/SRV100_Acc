import { router } from "../_core/procedures";

/**
 * AttendanceRouter - TRPC router for attendance module
 * Procedures will be added in subsequent phases
 */
export const attendanceRouter = router({});
